# REST-Api

A minimal REST API built using Node.js, Express, and MongoDB (via Mongoose).
Currently supports User CRUD operations.
This project is structured for future expansion, including product modules, validation, authentication, testing, and Docker support.

Features

.Basic REST API architecture
.User CRUD operations:
.Create user
.Get all users
.Get user by ID
.Update user
.Delete user
.MongoDB database connection using Mongoose
.Clean folder structure for scalability
